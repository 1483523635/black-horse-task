spring:
  flyway:
    enabled: true
  datasource:
    url: "jdbc:h2:mem:testdb;MODE=MYSQL"
    driverClassName: org.h2.Driver
    username: sa
    password:

trip:
  service:
    url: http://localhost:22222